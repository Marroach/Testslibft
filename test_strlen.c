#include "libft.h"
#include <stdio.h>

int main()
{

	char *strn = NULL;
	char *test = "qwerty";
	char *test = "1";
printf("Lenth is %zu ft \n", ft_strlen(str));
printf("Lenth is %lu libc \n", strlen(str));
}